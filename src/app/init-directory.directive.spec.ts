import { InitDirectoryDirective } from './init-directory.directive';

describe('InitDirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new InitDirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
